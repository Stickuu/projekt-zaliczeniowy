<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".fragments.AccountSettingsFragment">

    <com.google.android.material.switchmaterial.SwitchMaterial
        android:id="@+id/accountSettingsDarkModeSwitch"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/darkMode"
        android:textSize="20sp"
        android:layout_marginTop="20dp"
        android:layout_marginHorizontal="26dp"/>

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/languageText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/accountSettingsDarkModeSwitch"
        android:text="@string/languageText"
        android:layout_marginHorizontal="26dp"
        android:textSize="14sp"
        android:layout_marginTop="10dp"/>

    <Spinner
        android:id="@+id/languageSpinner"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/languageText"
        android:layout_marginTop="5dp"
        android:layout_marginHorizontal="18dp"/>

</RelativeLayout>